package org.edgegallery.appstore.domain.model.appd;

public class MfFileHandler {

    // IParamsHandler paresFlag(String line) {
    //     if (line.startsWith(MfMetaDataDef.startFlag)) {
    //         return new MfMetaDataDef();
    //     }
    //     if (line.startsWith(MfSourceFileDef.startFlag)) {
    //         return new MfSourceFileDef();
    //     }
    //     return null;
    // }

    public boolean delFileDescByName(String sourceName) {
        return false;
    }

    public boolean formatCheck() {
        return false;
    }
}


